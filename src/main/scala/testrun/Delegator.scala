package testrun
import scala.actors.Actor
import java.net.InetAddress

/*object Delegator {
    val id = 'deliverMathDelegator
    val port = 9000
    
    val delegator = new Delegator(id, port)
    
    def main(args: Array[String]) {
        delegator.start
        println("Server(" + InetAddress.getLocalHost().getHostAddress() + ", " + id + ") listening on port: " + port)
    }
}*/

/*class Delegator(val id: Symbol, val port: Int) extends Actor {
    import Delegator._
    
    // classLoader = getClass().getClassLoader
}*/